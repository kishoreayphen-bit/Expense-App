# 🚀 Quick Fix for Claims Tab

## ❌ **Root Cause Found**

The JWT token generated by the backend **does not include the user's role**. It only includes:
- `email`
- `userId`

The role is NOT in the token, so the mobile app can't extract it during login.

---

## ✅ **Quick Manual Fix**

Since you're logged in as `manager1@expense.app` and we know from the database that this user has role `MANAGER`, let's manually set it:

### **Run this command to set the role:**

```bash
# On Android Emulator, use adb shell
adb shell run-as com.expenseapp.mobile

# Then run this to set the role in AsyncStorage
# (This is a workaround - you'll need to do this via the app's dev menu or console)
```

---

## 🔧 **Better Solution: Add Role to JWT Token**

The proper fix is to add the role to the JWT token when it's generated. Let me implement this:

